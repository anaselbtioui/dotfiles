# Qt apps take colors, fonts and dialogs from GTK instead of shipping their own
# light Fusion look. Requires qt6-gtk-platformtheme and qt5-gtk-platformtheme.
QT_QPA_PLATFORMTHEME=gtk3

# Electron 28+ picks Wayland natively without per-app flags files.
ELECTRON_OZONE_PLATFORM_HINT=auto
