# Sourced only by Plasma (startplasma), not by GNOME.
# environment.d sets QT_QPA_PLATFORMTHEME=gtk3 for the GNOME session; override
# here so Plasma and its Qt apps use native Breeze instead of libqgtk3.
export QT_QPA_PLATFORMTHEME=plasma
